<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">
    <link rel="shortcut icon" href="/assets/ico/favicon.ico">

    <title>{{$title}}Linch{{/title}}</title>

    <link href="{{#path}}/assets/css/bootstrap.min.css{{/path}}" rel="stylesheet">
    <link href="{{#path}}/assets/css/linch.css{{/path}}" rel="stylesheet">
</head>

<body>

<div class="navbar navbar-inverse navbar-fixed-top" role="navigation">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="/">Linch</a>
        </div>

        <div class="collapse navbar-collapse">
            <!--
            <ul class="nav navbar-nav">
                <li class="active"><a href="#">Home</a></li>
                <li><a href="#about">About</a></li>
                <li><a href="#contact">Contact</a></li>
            </ul>
            -->

            {{^user}}
                {{^hideNavigationLogin}}
                    <form class="navbar-form navbar-right" role="form" action="{{#path}}/login{{/path}}" method="post">
                        <div class="form-group">
                            <input type="text" placeholder="Username" class="form-control" name="username">
                        </div>
                        <div class="form-group">
                            <input type="password" placeholder="Password" class="form-control" name="password">
                        </div>
                        <input type="hidden" name="next" value="{{route.url}}">
                        <input class="btn btn-success" type="submit" name="submit" value="Sign in">
                    </form>
                {{/hideNavigationLogin}}
            {{/user}}

            {{#user}}
                <ul class="nav navbar-nav navbar-right">
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown">{{user.fullName}}{{^user.fullName}}{{user.username}}{{/user.fullName}} <b class="caret"></b></a>
                        <ul class="dropdown-menu" role="menu">
                            <li><a href="{{#path}}/profile{{/path}}">Profile</a></li>
                            <li><a href="{{#path}}/admin{{/path}}">Admin</a></li>
                            <li class="divider"></li>
                            <li><a href="{{#path}}/logout{{/path}}">Logout</a></li>
                        </ul>
                    </li>
                </ul>
            {{/user}}
        </div>
    </div>
</div>

<div class="container">
    <div class="content">

        {{$content}}{{/content}}

    </div>
</div>

<script src="{{#path}}/assets/js/jquery-1.11.0.min.js{{/path}}"></script>
<script src="{{#path}}/assets/js/bootstrap.min.js{{/path}}"></script>
</body>
</html>
